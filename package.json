{
  "name": "photo-sharing-v1",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo: Express API (backend) + Create React App (frontend)",
  "scripts": {
    "setup:env": "node scripts/ensure-env.js",
    "postinstall": "node scripts/ensure-env.js",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm start --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run start:backend\" \"npm run start:frontend\"",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
